language: C

cache:
  timeout: 1800  # 30 mins
  directories:
    - Download
    - build

stages:
  - build
  - test
  - distribution

os:
#  - windows
  - linux

dist: bionic

matrix:
  include:
  - os: linux
    env:
      - HDF5_ROOT= STAGE=build
  - os: linux
    env:
      - HDF5_ROOT= STAGE=test
  - os: linux
    env:
      - HDF5_ROOT= STAGE=dist
#  - os: windows
#    env:
#      - MATRIX_EVAL="HDF5_ROOT=./Download/hdf5"

before_install: ci/bin/travis.sh before_install

script: ci/bin/travis.sh $STAGE

