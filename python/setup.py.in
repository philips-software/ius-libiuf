import io
import os
import setuptools.command.install
import ${LIBRARY_NAME}


def read(*filenames, **kwargs):
    encoding = kwargs.get('encoding', 'utf-8')
    sep = kwargs.get('sep', os.linesep)
    buf = []
    for filename in filenames:
        with io.open(filename, encoding=encoding) as f:
            buf.append(f.read())
    return sep.join(buf)


if __name__ == '__main__':
    setuptools.setup(
        name='${LIBRARY_NAME}',
        version=${LIBRARY_NAME}.__version__,
        description='IUF file IO library SDK',
        long_description=read('README.md'),
        long_description_content_type='text/markdown',
        author='IUF SDK Team',
        author_email='al_libiuf@natlab.research.philips.com',
        url='https://github.com/philips-software/ius-libiuf',
        license='MIT',
        packages=[''],
        package_dir={'': '.'},
        package_data={'': ['${SWIG_LIBIUF_SO}', '${SWIG_LIBIUF_PY}']},
        # See: https://pypi.org/classifiers/
        classifiers=[
            'Development Status :: 4 - Beta',
            'Intended Audience :: Science/Research',
            'License :: OSI Approved :: MIT License',
            'Operating System :: MacOS :: MacOS X',
            'Operating System :: POSIX :: Linux',
            'Operating System :: Microsoft :: Windows',
            'Programming Language :: C',
            'Programming Language :: Python :: 3.7'
        ]
)
